#include "test.h"

// program to check if state needs to be restored
// exit code is 0 if no state is present
// exit code is >0 if some state needs to be restored

int main()
{
	return state_present();
}
